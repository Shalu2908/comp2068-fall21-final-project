<h1> {{title}}</h1>
{{!-- Add links to CRUD operations --}}
<a href="/categories/addCategory" class="btn btn-info">Add a new category</a>

{{!-- To Show data on  the PatientsList Page--}}
<table class="table table-striped table-hover" style="font-family:Georgia, Garamond, Serif;color: rgb(255, 136, 0); font-style: italic; font-size:30px">
    <thead>
    <th>Name</th>
    </thead>
    {{#each dataset}}
        <tr>
            <td>{{this.name}}</td>
        </tr>
    {{/each}}
</table>